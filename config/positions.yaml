# Local position tracking (manual updates).
# Keep `positions` empty until live holdings are configured.

as_of: "2026-03-10"
base_currency: "USD"
thematic_sleeve:
  target_weight_pct:
  cash_reserve_pct: 30.0
positions: []
